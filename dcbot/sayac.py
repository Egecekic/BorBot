import discord
from discord.ext.commands import Bot
from discord.ext import commands
import asyncio


def karıSayacı(deneme):
    kari_sayaci = 0
    if message.content.upper() == "KARI":
        kari_sayaci +=1
    return kari_sayaci
